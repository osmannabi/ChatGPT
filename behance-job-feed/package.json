{
  "name": "behance-job-feed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Scrape recent Behance jobs with a logged-in browser, score them for 35milimetre, push the promising ones to Notion.",
  "scripts": {
    "login": "node run.mjs --login",
    "scan": "node run.mjs",
    "dry": "node run.mjs --dry-run",
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
